# Dependencies
node_modules/
**/node_modules/

# Environment files — NEVER commit these
.env
**/.env
.env.local
.env.production

# Supabase / Google service account keys
**/config/service-account.json

# Build outputs
dist/
build/
.expo/
**/.expo/

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
.claude/
.mcp.json

# Claude Code project guidance (AI context files — not for source control)
CLAUDE.md
PROJECT_CONTEXT.md

# PII reports / exports & local diagnostics — NEVER commit (contain employee data)
*_report.html
*_analysis.html
employees_list.csv
database/check_*.sql

# Stray root lockfile (real lockfiles live in each app subdir)
/package-lock.json
